#include <iostream>
#include <vector>

int main(void){
    // recall taht the order matters in this case, hence permutations.
    // check all coins per each sum value as this allows us to "choose" in all orders for solutions.
    // as such we create all permutations as we count up!


    return 0;
}